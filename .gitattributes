# Text files use LF everywhere (scripts must survive: .sh breaks on CRLF).
*.sh text eol=lf
# Windows batch/PowerShell keep CRLF.
*.cmd text eol=crlf
*.ps1 text eol=crlf
# Everything else: native Git handling.
